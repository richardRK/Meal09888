import React from "react";
import { TochableOpacity, View, Text, StyleSheet } from "react-native";

const CategoryGridTitle = (props) => {};

const styles = StyleSheet.create({});

export default CategoryGridTitle;
